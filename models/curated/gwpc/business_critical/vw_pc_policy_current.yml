version: 2

models:
  - name: vw_pc_policy_current
    description: "Policy model"

    columns:
      - name: id
        description: "Internally managed primary key."
        data_tests:
          - unique:
              name: curated_pc_policy_id_unique
              config:
               severity: warn
               tags: ['severe']
